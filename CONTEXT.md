# 当前进度

- 已完成可拖拽本地文件的 Spark 3DGS Viewer，并把交互面板改成悬浮的 liquid glass 风格。
- 页面同时支持远程 URL 和本地文件两种打开方式。

# 上次停在哪里

- 远程示例和本地拖拽加载都已跑通。
- 最新版本已经完成悬浮玻璃面板样式，并截图确认了页面效果。

# 关键决定

- 继续保留 Vite + 原生 JavaScript，先保证功能直接可用。
- 本地文件采用拖拽和文件选择两种入口，减少用户操作成本。
- 本地文件模式不写入 URL，只显示当前文件说明；远程模式继续保留分享链接能力。
- 参考 Liquid Glass 风格，把控制面板做成浮在画布上的半透明玻璃卡片，并加入高光、环境光斑和更强的模糊层次。
